{
  "name": "token-connector",
  "version": "1.0.0",
  "description": "Figma plugin to detect and fix remote/broken variable references by connecting them to local variables",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "typescript": "^5.4.0"
  }
}
